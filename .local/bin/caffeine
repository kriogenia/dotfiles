#!/usr/bin/env bash

FLAG=$HOME/.caffeine_active

if [[ -e "$FLAG" ]]; then
  echo "caffeine off"
  rm "$FLAG"
  killall hypridle
  hypridle >/dev/null &
else
  echo "☕ TIME"
  touch "$FLAG"
  killall hypridle
  hypridle -c "$XDG_CONFIG_HOME"/hypr/caffeine.conf >/dev/null &
fi
